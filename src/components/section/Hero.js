import React from "react";

const Hero = () => {
  return (
    <div
      className="hero relative h-screen flex items-center justify-center text-center bg-cover bg-center"
    ></div>
  );
};

export default Hero;
